name: Deploy QA to cPanel

on:
  push:
    branches:
      - main # O puedes cambiarlo a 'qa' si usas otra rama para pruebas

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Sync files to cPanel FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /public_html/qa.quinsacchile.cl/ # Ruta exacta de tu subdominio
